test.each([ 40, 42, 100 ] as const)('top shape renders radio buttons', _size => {

})


test.todo('collage shape renders radio buttons and sliders')


test.each([ 40, 42, 100 ] as const)('clicking top radio buttons dispatches action', _size => {

})

test.todo('clicking collage radio button dispatches action')


test.each([ 'x', 'y' ] as const)('moving collage sliders dispatches action', _axis => {

})
